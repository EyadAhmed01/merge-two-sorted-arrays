#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n,i;
    printf("enter size of the array");
    scanf("%d",&n);
int a[n];
for(i=0;i<n;i++)
{
    printf("enter a[%d];",i);
    scanf("%d",&a[i]);

}
}
